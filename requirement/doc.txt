https://github.com/Larry-Volz/theComicSwap_elephantSQL_and_flask_version

https://larry-volz.com/


psql -U postgres  
\l  //show databases
\c comicswap2  //enter this database

\dt //show relations
\q  //exit

CREATE DATABASE comicswap2;   // create database
DROP DATABASE comicswap2  // delete database


flask run
flask db 
flask shell 
flask routes

# Create all tables
with app.app_context():   //database connection with context neccessary
    db.drop_all()
    db.create_all()

psql -U postgres -d dvdrental //In this command, the -d flag means database. In this command, you connect to the dvdrental database using the postgres user.

 flask run -h localhost -p 80

pip install gunicorn
pip install psycopg2
pip freeze > requirements.txt
echo web: gunicorn app:app > Procfile
heroku login
heroku create comicswap2
heroku addons:create heroku-postgresql:hobby-dev --app comicswap2
heroku config --app comicswap2
git add .
git commit -am "tweaked app for heroku"
git push
git push heroku master

heroku run python
from app import db
db.create_all()


pip install wheel
python app.py bdist_wheel